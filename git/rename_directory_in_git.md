How to remane a directory in Git?

Basic rename (or move)

```python
git mv <old name> <new name>
```

Case Sensitive rename you need two steps
```python
git mv casesensitive tmp
git mv tmp CaseSensitive
```
